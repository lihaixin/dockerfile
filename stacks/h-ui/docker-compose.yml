services:
  h-ui:
    image: jonssonyan/h-ui
    privileged: true
    container_name: h-ui
    restart: always
    network_mode: host
    volumes:
      - h-ui:/h-ui
    environment:
      TZ: Asia/Shanghai
      
volumes:
  h-ui:
